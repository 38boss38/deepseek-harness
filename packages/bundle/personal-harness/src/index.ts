/**
 * @deepseek-ai/dsh-personal-harness — personal routing, playbook, and memory
 * bundle. Runtime behavior lives in the three packages inserted by the bundle's
 * `cordis.patch.yml`; this module carries no runtime API.
 * @module @deepseek-ai/dsh-personal-harness
 */

export {}
